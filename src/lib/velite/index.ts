// .velite/ is generated at build time by running `velite` (or `pnpm build`).
// Run `pnpm build` or `npx velite` once to generate the content module.
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore – generated at build time
export { posts, projects } from '../../../.velite'
